import { GlobalThemeOverrides } from 'naive-ui'

export const THEME_OVERRIDES_SMALL_SIZED: GlobalThemeOverrides = {
  Message: {
    padding: '4px 8px',
    fontSize: '12px',
    iconSize: '16px',
    iconMargin: '0 4px 0 0'
  }
}
